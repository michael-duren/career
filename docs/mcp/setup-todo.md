# Setup TODO

- [ ] Deploy a release containing the Go MCP server (migration 006 runs with the release).
- [ ] Confirm `https://career.duckgc.com/.well-known/oauth-authorization-server` and
      `/api/mcp` (expect 401 with `WWW-Authenticate`) are publicly reachable through the tunnel.
- [ ] Add the Claude custom connector and approve on the consent page.
- [ ] Add the server to Claude Code and authenticate with `/mcp`.
- [ ] Call all four tools from each client.

See [MCP setup](README.md).
